<% @routes.each do |route| %>
  <%= link_to "#{route.short_name}: #{route.long_name}", route_map_path(route: route.short_name), class: "btn btn-lg btn-block", style: "background-color: \##{route.color}; color: \##{route.text_color};" %>
<% end %>