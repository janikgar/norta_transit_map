<nav class="nav navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="glyphicon glyphicon-menu-hamburger"></span>
      </button>
      <a class="navbar-brand" href="#">NORTA Finder</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li>
            <% if current_user.provider == "facebook" %>
              <%= image_tag(current_user.normalimage) %>
            <% else %>
              <%= image_tag(current_user.image, class: "img-circle pull-left", style: "max-width:50px; max-height:50px; height: auto;") %><br />
            <% end %>
            <p class="navbar-text">Welcome, <strong><%= current_user.name %>!</strong></p>
          </li>
          <li><%= link_to "Sign Out", signout_path, id: "sign_out" %></li>
        <% else %>
          <li>
            <%= link_to "Sign In with Facebook", "/auth/facebook", id: "sign_in" %>
          </li>
          <li>
            <%= link_to "Sign In with Github", "/auth/github", id: "sign_in" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>